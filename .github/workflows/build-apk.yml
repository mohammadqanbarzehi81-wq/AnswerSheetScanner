name: Build APK
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "APK would be built here"
      - uses: actions/upload-artifact@v4
        with:
          name: test-apk
          path: README.md
